# Дорожная карта разработки hashcat2 — secp256k1

> **Язык:** Русский (техническая документация)  
> **Последнее обновление:** 2026-03-06  
> **Статус:** В работе

---

## Обзор

Данная дорожная карта описывает поэтапный план оптимизации реализации
эллиптической кривой secp256k1 в проекте hashcat2 с целью максимизации
производительности GPU при взломе Bitcoin/Ethereum brainwallet-паролей.

**Текущая скорость (baseline):** ~706 kH/s (RTX 3090, тестовый стенд)  
**Целевая скорость:** ≥ 2 800 kH/s (+4× к baseline)

---

## Фаза 0 — Подготовка и анализ (ЗАВЕРШЕНО ✅)

| Задача | Статус | Файл |
|---|---|---|
| Исходный анализ кода `inc_ecc_secp256k1.cl` | ✅ | `SECP256K1_ANALYSIS.md` |
| Идентификация точек расхождения потоков (warp divergence) | ✅ | `OPTIMIZATION_SUMMARY.md` |
| Документирование функций `mul_mod`, `sqr_mod`, `inv_mod` | ✅ | `docs/SECP256K1_OPTIMIZATION_PLAN_RU.md` |
| Создание тестовых хешей для модулей 35900–35904 | ✅ | `tools/` |

---

## Фаза 1 — Базовые оптимизации (ЗАВЕРШЕНО ✅)

**Цель:** Устранить прямые потери производительности без изменения алгоритмического уровня.

### 1.1 Специализированная функция возведения в квадрат `sqr_mod`

| Метрика | До | После | Прирост |
|---|---|---|---|
| Умножений на вызов | 64 | ~36 | −43 % |
| Мест вызова | 7 | 7 | — |
| Расхождение потоков | нет | нет | — |

- **Реализация:** `OpenCL/inc_ecc_secp256k1.cl`, строки 746–947
- **Прототип:** `OpenCL/inc_ecc_secp256k1.h`
- **Затронутые функции:** `sqrt_mod` (1×), `point_double` (4×), `point_add` (3×)

### 1.2 Инверсия по Ферма `inv_mod`

| Метрика | До (Binary EGCD) | После (Fermat) | Прирост |
|---|---|---|---|
| Итераций | ~512 (переменное) | 256 (фиксированное) | −50 % |
| Ветвлений | 3+ на итерацию | **0** | −100 % |
| Warp divergence | тяжёлое | **нет** | ✅ |

- **Реализация:** `OpenCL/inc_ecc_secp256k1.cl`, строки 1000–1103
- **Метод:** a⁻¹ ≡ a^(p−2) mod p (Малая теорема Ферма)

**Ожидаемый суммарный прирост Фазы 1:** ~2–3× к baseline

---

## Фаза 2 — GLV-эндоморфизм (ПЛАНИРУЕТСЯ 🔲)

**Цель:** Сократить скалярное умножение вдвое за счёт разложения скаляра k на
два 128-битных подскаляра (k₁, k₂) с использованием собственного эндоморфизма
кривой secp256k1.

### 2.1 Теоретическое основание

Кривая secp256k1 допускает эффективный эндоморфизм φ: (x, y) → (βx, y),
где β — кубический корень из 1 в F_p.

```
λ = 0x5363AD4CC05C30E0A5261C028812645A122E22EA20816678DF02967C1B23BD72
β = 0x7AE96A2B657C07106E64479EAC3434E99CF0497512F58995C1396C28719501EE
```

Для скаляра k разложение: k = k₁ + k₂·λ (mod n), где |k₁|, |k₂| < 2¹²⁸.

### 2.2 Шаги реализации

- [ ] Реализовать функцию `glv_decompose(k, k1, k2)` — разложение скаляра
- [ ] Реализовать `point_mul_glv(x, y, k1, k2, preG, preGphi)` — двойное умножение
- [ ] Предвычислить точку φ(G) = (β·Gx mod p, Gy)
- [ ] Интегрировать с w-NAF оконным методом (joint sparse form)
- [ ] Обновить `point_mul_xy` для использования GLV-пути
- [ ] Тестировать корректность на тестовых векторах

### 2.3 Ожидаемый прирост

| Операция | Текущие издержки | С GLV | Прирост |
|---|---|---|---|
| Скалярное умножение | 256 двоичных шагов | ~128 шагов | ~2× |
| Общий hashrate | ~2 100 kH/s | ~3 500 kH/s | ~+67 % |

**Файлы для модификации:**
- `OpenCL/inc_ecc_secp256k1.cl` — добавить GLV-функции
- `OpenCL/inc_ecc_secp256k1.h` — добавить константы λ, β

---

## Фаза 3 — Продвинутые операции в поле (ПЛАНИРУЕТСЯ 🔲)

**Цель:** Ускорить низкоуровневые арифметические операции в GF(p).

### 3.1 PTX inline assembly (только NVIDIA)

- [ ] Реализовать `mul_mod_ptx` с использованием инструкций `mad.lo`, `mad.hi`
- [ ] Реализовать `add_mod_ptx` с `add.cc`, `addc`
- [ ] Создать `#ifdef CUDA_ARCH` обёртку для условной компиляции
- [ ] Бенчмарк PTX vs OpenCL C на RTX 3090/4090

**Ожидаемый прирост:** +15–25 % на NVIDIA GPU

### 3.2 Ручное развёртывание циклов

- [ ] Развернуть внутренние циклы в `mul_mod` (строки 593–744)
- [ ] Развернуть цикл в `inv_mod` (256 итераций → явные блоки по 16)
- [ ] Оценить trade-off: размер инструкций vs ILP (instruction-level parallelism)

**Ожидаемый прирост:** +5–10 %

### 3.3 Оптимизация кривой (a=0, b=7)

- [ ] Упростить `point_double` с учётом a=0: исключить 1 умножение
- [ ] Убедиться, что все `a·Z⁴` члены оптимизированы компилятором или удалены вручную
- [ ] Применить формулы из Bernstein & Lange "Explicit Formulas Database"

**Ожидаемый прирост:** +3–8 %

### 3.4 Пакетная инверсия Монтгомери

- [ ] Реализовать `batch_inv_mod(arr, n)` для инверсии n элементов
- [ ] Применить в `point_get_coords` при предвычислении кратных точек
- [ ] Тест: 4 одиночных инверсии vs 1 пакетная (цена: 1 inv + 3n mul)

**Ожидаемый прирост:** +10–20 % при высоком параллелизме

---

## Фаза 4 — Новые модули (ПЛАНИРУЕТСЯ 🔲)

**Цель:** Расширить список поддерживаемых форматов атак.

### 4.1 Модуль 35910 — Bitcoin Brainwallet (Blake2b-256)

| Параметр | Значение |
|---|---|
| Номер модуля | 35910 |
| Хеш-функция | BLAKE2b-256 |
| Адресные форматы | P2PKH / Bech32 / P2SH |
| Статус | ❌ Не реализован |

- [ ] Написать `OpenCL/m35910_a0-pure.cl`, `a1-pure.cl`, `a3-pure.cl`
- [ ] Написать `modules/module_35910.c`
- [ ] Добавить тестовые хеши в `tools/` и `example35910.hash`

**Конвейер:**
```
passphrase → BLAKE2b-256 → [private_key 32B] → ECC G·k → compressed_pubkey
→ SHA256 → RIPEMD160 → [hash160 20B] → BASE58CHECK / BECH32 → address
```

### 4.2 Модуль 35912 — Ethereum Brainwallet (Blake2s-256)

| Параметр | Значение |
|---|---|
| Номер модуля | 35912 |
| Хеш-функция | BLAKE2s-256 |
| Адресный формат | Ethereum (Keccak-256 последних 20 байт pubkey) |
| Статус | ❌ Не реализован |

- [ ] Написать `OpenCL/m35912_a0-pure.cl`, `a1-pure.cl`, `a3-pure.cl`
- [ ] Написать `modules/module_35912.c`
- [ ] Добавить тестовые хеши

**Конвейер:**
```
passphrase → BLAKE2s-256 → [private_key 32B] → ECC G·k → uncompressed_pubkey[64B]
→ Keccak-256 → last 20 bytes → "0x" + HEX → Ethereum address
```

---

## Фаза 5 — Финальная оптимизация и бенчмаркинг (ПЛАНИРУЕТСЯ 🔲)

**Цель:** Верификация всех улучшений, создание эталонных результатов, публикация.

- [ ] Полный прогон тестов: все режимы a0, a1, a3 для модулей 35900–35904, 35910, 35912
- [ ] Бенчмарк на GPU: RTX 3090, RTX 4090, RX 7900 XTX, A100
- [ ] Профилирование: NSight Compute / RGP для выявления узких мест
- [ ] Документирование финальных показателей hashrate
- [ ] Сравнение с keyhunt, ice_poseidon2, bitcoin-core reference
- [ ] Обновление README с итоговыми цифрами
- [ ] Публикация CHANGELOG v1.0.0

---

## Сводная таблица ожидаемого прироста

| Фаза | Оптимизация | Ожидаемый прирост | Накопленный |
|---|---|---|---|
| 1 | sqr_mod + Fermat inv_mod | +2–3× | ~2–3× |
| 2 | GLV-эндоморфизм | +50–70 % | ~3–5× |
| 3 | PTX + unroll + a=0 + batch inv | +30–50 % | ~4–7.5× |
| 4 | Новые модули 35910, 35912 | новые форматы | — |
| 5 | Тюнинг + верификация | +5–10 % | ~4–8× |

**Итоговый прогноз:** ~4–8× ускорение относительно оригинального hashcat.

---

## Зависимости между фазами

```
Фаза 0 ──▶ Фаза 1 ──▶ Фаза 2 ──▶ Фаза 5
                   └──▶ Фаза 3 ──▶ Фаза 5
                   └──▶ Фаза 4 ──▶ Фаза 5
```

Фазы 3 и 4 могут выполняться параллельно после завершения Фазы 1.

---

## Ссылки

- [SECP256K1_OPTIMIZATION_PLAN_RU.md](docs/SECP256K1_OPTIMIZATION_PLAN_RU.md)
- [MODULE_ANALYSIS_RU.md](docs/MODULE_ANALYSIS_RU.md)
- [GLV_EXTERNAL_REFS_RU.md](docs/GLV_EXTERNAL_REFS_RU.md)
- [OPTIMIZATION_SUMMARY.md](OPTIMIZATION_SUMMARY.md)
- [SECP256K1_ANALYSIS.md](SECP256K1_ANALYSIS.md)
